# Published coordinates. `version` must NOT end in -SNAPSHOT for a Maven Central release;
# bump it here and nowhere else.
group=net.bitsar
version=0.2.0

# Dependency versions, kept here so the starter and the demo can never drift apart.
springBootVersion=3.5.16
springDependencyManagementVersion=1.1.7
redissonVersion=3.52.0
springdocVersion=2.9.0

org.gradle.parallel=true
org.gradle.caching=true
